// here we will learn about higher order functions in js
// the fn which accept another fn as  parameter are called higher order functions

var arr = [1,2,3,5,6]

arr.forEach(function(){}); // here forEach is the higher order function which accept another function as parameter